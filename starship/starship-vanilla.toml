# Vanilla Starship Configuration (Stage 2)
# Minimal prompt with directory module

format = "$directory$character"

[directory]
style = "cyan bold"
truncation_length = 3
truncate_to_repo = true
format = "[$path]($style) "

[character]
success_symbol = "[❯](green)"
error_symbol = "[❯](red)"
