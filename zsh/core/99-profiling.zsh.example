#!/usr/bin/env zsh
# Shell profiling configuration
# Rename to 99-profiling.zsh to enable profiling

# This file should be loaded LAST in core/ (hence the 99- prefix)
# It outputs profiling results when shell starts

# Enable profiling by adding to TOP of .zshrc:
# zmodload zsh/zprof

# Then this will show results at shell startup:
if (( $+functions[zprof] )); then
    echo ""
    echo "=== ZSH Profiling Results ==="
    zprof | head -20
    echo ""
    echo "Full results: run 'zprof' command"
fi

# Or disable automatic display and run manually:
# zsh -i -c zprof
